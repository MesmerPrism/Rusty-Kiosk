<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:horizonos="http://schemas.horizonos/sdk"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">

    <horizonos:uses-horizonos-sdk
        horizonos:minSdkVersion="69"
        horizonos:targetSdkVersion="69" />

    <uses-feature
        android:name="android.hardware.vr.headtracking"
        android:required="true" />
    <uses-feature
        android:name="com.oculus.feature.PASSTHROUGH"
        android:required="false" />
    <uses-feature
        android:name="com.oculus.feature.VIRTUAL_KEYBOARD"
        android:required="false" />
    <uses-feature
        android:name="oculus.software.overlay_keyboard"
        android:required="false" />
    <uses-feature
        android:name="oculus.software.handtracking"
        android:required="false" />
    <uses-feature android:glEsVersion="0x00030001" />

    <!--
      Complete installed-app discovery is the user-facing purpose of Rusty Kiosk.
      The narrower launcher queries below do not expose installed packages that
      have no public front door, which must still be shown for imported tag files.
    -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />
    <!-- Granted only when the separately installed setup helper has the same signer. -->
    <uses-permission android:name="${setupControlPermission}" />
    <uses-permission android:name="com.oculus.permission.HAND_TRACKING" />
    <uses-permission android:name="org.khronos.openxr.permission.OPENXR" />
    <uses-permission android:name="org.khronos.openxr.permission.OPENXR_SYSTEM" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <queries>
        <package android:name="${setupHelperPackage}" />
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.INFO" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="com.oculus.intent.category.VR" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="com.oculus.intent.category.2D" />
        </intent>
    </queries>

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@drawable/ic_rusty_kiosk"
        android:label="${kioskLabel}"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.oculus.supportedDevices"
            android:value="quest2|questpro|quest3|quest3s" />
        <meta-data
            android:name="com.oculus.vr.focusaware"
            android:value="true" />
        <!-- The server consumes the shared constants but must not advertise itself as a client. -->
        <meta-data
            android:name="io.github.mesmerprism.rustykiosk.FOREGROUND_SIGNAL_PROTOCOL"
            tools:node="remove" />
        <uses-native-library
            android:name="libossdk.oculus.so"
            android:required="true" />

        <activity
            android:name=".RustyKioskActivity"
            android:configChanges="screenSize|screenLayout|orientation|keyboardHidden|keyboard|navigation|uiMode"
            android:excludeFromRecents="true"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            tools:ignore="DiscouragedApi">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.oculus.intent.category.VR" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".KioskAccessibilityService"
            android:description="@string/accessibility_service_description"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:stopWithTask="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/kiosk_accessibility_service" />
        </service>

        <!--
          Explicitly wearer-enabled, bounded local operator bridge. It exposes no shell or
          arbitrary Android intents. Authentication is application-level HMAC with expiry and
          replay protection; the setup helper remains network-free.
        -->
        <service
            android:name=".OperatorBridgeService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="false" />

        <receiver
            android:name=".RustyKioskInstallReceiver"
            android:exported="false" />

        <!--
          Fixed typed host operations for an already authorized ADB shell. Ordinary headset apps
          cannot hold android.permission.DUMP, and this provider supports call() only.
        -->
        <provider
            android:name=".RustyKioskOperatorProvider"
            android:authorities="${operatorAuthority}"
            android:exported="true"
            android:permission="android.permission.DUMP" />

        <!--
          Engine-neutral advisory fast path. Runtime admission binds the immediate Binder caller
          to the armed package, exclusive UID, protocol metadata, and launch-time signing lineage.
        -->
        <provider
            android:name=".ForegroundSignalProvider"
            android:authorities="${foregroundSignalAuthority}"
            android:exported="true" />
    </application>
</manifest>
